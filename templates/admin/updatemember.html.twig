{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        {{ include("dashboard/admin.html.twig") }}
        {{ form_start(userForm) }}
        {{ form_widget(userForm) }}
        {{ form_end(userForm) }}
    </div>
{% endblock %}
