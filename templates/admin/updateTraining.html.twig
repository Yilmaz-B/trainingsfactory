{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        {{ include("dashboard/admin.html.twig") }}
        {{ form_start(updateForm) }}
        {{ form_widget(updateForm) }}
        {{ form_end(updateForm) }}
    </div>
{% endblock %}
